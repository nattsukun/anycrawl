# คู่มือการใช้งาน HotelPlus Blog Scraper

## 📋 คำอธิบาย
Workflow n8n สำหรับดึงข้อมูลบทความทั้งหมดจากเว็บไซต์ https://www.hotelplus.asia/blog และแปลงเป็น CSV หรือบันทึกลงฐานข้อมูล PostgreSQL

## ✨ ฟีเจอร์หลัก

- ✅ ดึงข้อมูลจาก**ทุกหน้าของ Blog** (Pagination)
- ✅ ลบ URL ซ้ำอัตโนมัติ
- ✅ แยกข้อมูลจาก Metadata HTML
- ✅ ตรวจจับภาษาอัตโนมัติ (ไทย/อังกฤษ)
- ✅ ระบุสถานที่อัตโนมัติ (เชียงใหม่, ภูเก็ต, กรุงเทพ ฯลฯ)
- ✅ จัดหมวดหมู่ตามเนื้อหา (โรงแรม, รีวิว, กลยุทธ์)
- ✅ Export เป็น CSV พร้อม Timestamp
- ✅ บันทึกลงตาราง PostgreSQL

## 🗂️ โครงสร้างข้อมูลที่ได้

| คอลัมน์ | คำอธิบาย | ตัวอย่าง |
|---------|----------|----------|
| `id` | รหัสบทความ | 1, 2, 3... |
| `type` | ประเภท | keyword, review, faq, guide |
| `topic` | หัวข้อหลัก | เชียงใหม่, ทั่วไป, กลยุทธ์ |
| `keyword` | คำสำคัญ | best hotel Chiang Mai |
| `description` | รายละเอียด | คำอธิบายบทความ |
| `reference_id` | รหัสอ้างอิง | blog1_2026 |
| `url` | URL บทความ | https://www.hotelplus.asia/blog/... |
| `category` | หมวดหมู่ | โรงแรม, รีวิว, ความรู้ทั่วไป |
| `location` | สถานที่ | เชียงใหม่, ภูเก็ต, กรุงเทพ |
| `date` | วันที่เผยแพร่ | 2026-01-29 |
| `language` | ภาษา | TH, EN |
| `search_volume` | ปริมาณการค้นหา | (ว่างไว้เพื่อกรอกภายหลัง) |
| `competition` | ความแข่งขัน | (ว่างไว้เพื่อกรอกภายหลัง) |
| `ranking` | อันดับ | (ว่างไว้เพื่อกรอกภายหลัง) |
| `notes` | หมายเหตุ | Scraped from hotelplus.asia... |

## 🚀 วิธีการติดตั้งและใช้งาน

### 1. Import Workflow เข้า n8n

```bash
# เปิด n8n
npm start

# หรือใช้ Docker
docker-compose up -d
```

1. เปิด n8n UI (โดยปกติอยู่ที่ http://localhost:5678)
2. คลิก **Import from File**
3. เลือกไฟล์ `hotelplus-blog-scraper-cheerio.json`
4. คลิก **Import**

### 2. ตั้งค่า PostgreSQL (ถ้าต้องการบันทึกลง Database)

#### สร้าง Credentials
1. ไปที่ **Credentials** → **New**
2. เลือก **PostgreSQL**
3. กรอกข้อมูล:
   - Host: `localhost` หรือ IP เซิร์ฟเวอร์
   - Database: `hotelplus_analytics`
   - User: `postgres`
   - Password: รหัสผ่านของคุณ
   - Port: `5432`
4. คลิก **Save**

#### เชื่อมต่อกับ Workflow
1. เปิดโหนด **"Create Table (If Not Exists)"**
2. เลือก Credentials ที่สร้างไว้
3. ทำซ้ำกับโหนด **"Insert to Database"**

### 3. ปรับแต่งการตั้งค่า

#### เปลี่ยนจำนวนหน้าที่ต้องการดึง
เปิดโหนด **"Generate Page URLs"** แก้โค้ด:

```javascript
const maxPages = 10; // เปลี่ยนเป็น 20, 50 หรือตามต้องการ
```

#### เปลี่ยน Path สำหรับบันทึก CSV
เปิดโหนด **"Write CSV File"** แก้:

```
C:\Users\NMuan\Downloads\hotelplus\hotelplus_metadata_{{ $now.format('yyyyMMdd_HHmmss') }}.csv
```

#### เพิ่มสถานที่ใหม่
เปิดโหนด **"Map to Metadata Format"** เพิ่มใน `locationMap`:

```javascript
const locationMap = {
  'เชียงใหม่': 'เชียงใหม่',
  'chiangmai': 'เชียงใหม่',
  'ภูเก็ต': 'ภูเก็ต',
  'phuket': 'ภูเก็ต',
  'กรุงเทพ': 'กรุงเทพ',
  'bangkok': 'กรุงเทพ',
  // เพิ่มสถานที่ใหม่ที่นี่
  'หัวหิน': 'หัวหิน',
  'hua hin': 'หัวหิน',
};
```

### 4. รัน Workflow

1. คลิกปุ่ม **Execute Workflow**
2. รอให้ดำเนินการเสร็จ (อาจใช้เวลานานขึ้นอยู่กับจำนวนบทความ)
3. ตรวจสอบผลลัพธ์:
   - **CSV File**: ที่โฟลเดอร์ `C:\Users\NMuan\Downloads\hotelplus\`
   - **Database**: ตรวจสอบตาราง `metadata` ใน PostgreSQL

## 🔄 Flow การทำงาน

```
📍 Start
  ↓
📄 Generate Page URLs (สร้าง URL หน้า 1-10)
  ↓
🌐 Get Blog List Page (ดึงข้อมูลแต่ละหน้า)
  ↓
✅ Check Page Has Articles (เช็คว่ามีบทความไหม)
  ↓
🔗 HTML - Extract Links (ดึง URL ของบทความ)
  ↓
🧹 Process URLs (กรองและทำความสะอาด)
  ↓
🔄 Remove Duplicate URLs (ลบ URL ซ้ำ)
  ↓
🔢 Re-index URLs (เรียงลำดับ ID ใหม่)
  ↓
🔁 Split In Batches (วนลูปแต่ละบทความ)
  ↓
  ├─▶ 📥 Get Article Content (ดึงเนื้อหาบทความ)
  │     ↓
  │   📊 Extract Article Metadata (แยก Title, Description, ฯลฯ)
  │     ↓
  │   🏷️ Map to Metadata Format (แปลงเป็นรูปแบบ Metadata)
  │     ↓
  │   🔄 Loop Back (วนกลับไปดึงบทความถัดไป)
  │
  └─▶ 📦 Aggregate All Articles (รวมข้อมูลทั้งหมด)
        ↓
        ├─▶ 💾 Convert to CSV → Write CSV File
        │
        └─▶ 🗄️ Create Table → Insert to Database
```

## ⚙️ การตั้งค่าขั้นสูง

### ปรับ Timeout สำหรับ Request
ในโหนด **"Get Article Content"** แก้:

```json
{
  "options": {
    "timeout": 30000  // 30 วินาที (เพิ่มได้ถ้าเน็ตช้า)
  }
}
```

### เพิ่ม Error Handling
เพิ่มโหนด **"Error Trigger"** เพื่อจัดการกับข้อผิดพลาด:

1. เพิ่มโหนด **"Error Trigger"**
2. เชื่อมต่อกับโหนด **"Send Email"** หรือ **"Slack"** เพื่อแจ้งเตือน

### เปลี่ยนรูปแบบ Pagination URL
ถ้าเว็บใช้รูปแบบอื่น แก้ในโหนด **"Generate Page URLs"**:

```javascript
// สำหรับ /blog/page/2
pageUrl: `https://www.hotelplus.asia/blog/page/${i}`

// สำหรับ /blog?p=2
pageUrl: `https://www.hotelplus.asia/blog?p=${i}`
```

## 📊 ตัวอย่าง SQL Query

### ดูข้อมูลทั้งหมด
```sql
SELECT * FROM metadata ORDER BY id;
```

### นับจำนวนบทความตามภาษา
```sql
SELECT language, COUNT(*) as count 
FROM metadata 
GROUP BY language;
```

### ดูบทความตามสถานที่
```sql
SELECT location, COUNT(*) as count 
FROM metadata 
WHERE location != '' 
GROUP BY location 
ORDER BY count DESC;
```

### ดูบทความรีวิว
```sql
SELECT id, keyword, url 
FROM metadata 
WHERE type = 'review';
```

### ค้นหาบทความเกี่ยวกับเชียงใหม่
```sql
SELECT id, keyword, category, url 
FROM metadata 
WHERE topic = 'เชียงใหม่' OR location = 'เชียงใหม่';
```

## 🐛 การแก้ไขปัญหา

### ปัญหา: โหนด "HTML - Extract Links" ไม่พบลิงก์
**วิธีแก้:**
1. ตรวจสอบ CSS Selector ใน Website
2. เปิด Developer Tools (F12) ที่เว็บไซต์
3. ดู HTML Structure ของลิงก์บทความ
4. แก้ CSS Selector ในโหนด **"HTML - Extract Links"**:
   ```
   a[href*='/blog/']  → เปลี่ยนตาม HTML จริง
   ```

### ปัญหา: เชื่อมต่อ Database ไม่ได้
**วิธีแก้:**
1. ตรวจสอบว่า PostgreSQL ทำงานอยู่
   ```bash
   # Windows
   services.msc → ดู "postgresql-x64-xx"
   
   # Linux/Mac
   sudo systemctl status postgresql
   ```
2. ตรวจสอบ Firewall (Port 5432)
3. ทดสอบเชื่อมต่อด้วย pgAdmin หรือ psql

### ปัญหา: ไม่พบไฟล์ CSV
**วิธีแก้:**
1. สร้างโฟลเดอร์ก่อน:
   ```bash
   mkdir C:\Users\NMuan\Downloads\hotelplus
   ```
2. ตรวจสอบ Permission การเขียนไฟล์

### ปัญหา: Workflow ช้า
**วิธีแก้:**
1. ลด `maxPages` จาก 10 เป็น 5
2. เพิ่ม `batchSize` ในโหนด **"Split In Batches"**
3. เพิ่ม Delay ระหว่าง Request:
   ```javascript
   // เพิ่มใน Code Node
   await new Promise(resolve => setTimeout(resolve, 1000)); // รอ 1 วินาที
   ```

## 📝 หมายเหตุ

- Workflow นี้ใช้ได้กับ n8n เวอร์ชัน 1.0.0 ขึ้นไป
- ต้องมี Node.js 18+ สำหรับโหนด Code
- รองรับ HTML Static Rendering (ไม่รองรับ JavaScript Rendering)
- สำหรับเว็บที่ใช้ JavaScript Rendering ให้ใช้ Playwright/Puppeteer แทน

## 🔗 ไฟล์ที่เกี่ยวข้อง

- `hotelplus-blog-scraper-cheerio.json` - Workflow หลัก (PostgreSQL)
- `hotelplus-blog-to-sqlite.json` - Workflow สำหรับ SQLite
- `hotelplus-blog-scraper-workflow.json` - Workflow แบบพื้นฐาน

## 📞 ติดต่อสอบถาม

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อ:
- Email: info@hotelplus.com
- Line: @hotelplus

## 📜 License

MIT License - ใช้งานได้อย่างอิสระ

---

**เวอร์ชัน:** 1.0.0  
**อัพเดทล่าสุด:** 29 มกราคม 2026  
**ผู้พัฒนา:** Kalodata Analytics Team
